paths-ignore:
  # excluded as false positive, this token is not valid and was just for testing purpose
  - "platform-apps/charts/argocd/templates/appset-github-token.yaml"
  # excluded as false positive, this token is not valid and was just for testing purpose
  - "platform-apps/charts/kubecost/values-k3d.yaml"
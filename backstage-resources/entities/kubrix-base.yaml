---
# locations
apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: demo-location
  description: A collection of all Backstage entities for official kubriX stack
spec:
  targets:
    - ./kubrix-base/*.yaml
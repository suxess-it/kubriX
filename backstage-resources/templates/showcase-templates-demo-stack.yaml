apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: software-templates-demo
  description: A collection of Backstage templates for kubriX
spec:
  type: url
  targets:
    - ./scaffolder-templates-demo-stack/**/template.yaml
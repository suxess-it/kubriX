apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    grafana_dashboard: "1"
  name: new-dashboard
  namespace: monitoring
data:
  new-dashboard.json: |-
{{ .Files.Get "dashboard-files/dashboard.json" | indent 4 }}
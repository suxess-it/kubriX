loki:
  gateway:
    ingress:
      hosts:
        - host: logs-monitoring.demo.kubrix.cloud
          paths:
            - path: /
              pathType: Prefix
      tls:
        - secretName: loki-gateway-tls
          hosts:
            - logs-monitoring.demo.kubrix.cloud

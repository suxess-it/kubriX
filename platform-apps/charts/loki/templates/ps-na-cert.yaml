apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: pushsecret-netapp-cert
  namespace: loki
spec:
  refreshInterval: 10s
  secretStoreRefs:
    - name: vault-backend
      kind: ClusterSecretStore
  selector:
    secret:
      name: s3-cert
  data:
    - match:
        secretKey: S3CERT
        remoteRef:
          remoteKey: observability/s3
          property: S3CERT
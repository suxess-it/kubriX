apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: crossplane-kubrix-service-status
  namespace: mimir
  labels:
    platform: kubrix
    service: crossplane
spec:
  groups:
  - name: kubrix.service.status
    interval: 30s
    rules:
    - record: kubrix_service_status
      labels:
        platform: kubrix
        service: crossplane
      expr: |
        (
          2 * (scalar(min(probe_success{platform="kubrix",service="crossplane"})) == bool 0)
        )
        +
        (
          scalar(max(GRAFANA_ALERTS{namespace="crossplane"} == bool 1) or vector(0))
          *
          (1 - (scalar(min(probe_success{platform="kubrix",service="crossplane"})) == bool 0))
        )
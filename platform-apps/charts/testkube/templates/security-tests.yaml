{{- if .Values.securityTests.enabled -}}
apiVersion: testworkflows.testkube.io/v1
kind: TestWorkflow
metadata:
  name: kind-security
spec:
  steps:
    - run:
        image: "curlimages/curl:7.78.0"
        shell: |
          curl https://vault.127-0-0-1.nip.io
{{- end }}

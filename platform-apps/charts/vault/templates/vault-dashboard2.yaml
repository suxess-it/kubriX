apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    grafana_dashboard: "1"
  name: vault-dashboard2
data:
  vault-dashboard.json: |-
{{ .Files.Get "dashboard-files/vault-dashboard2.json" | indent 4 }}

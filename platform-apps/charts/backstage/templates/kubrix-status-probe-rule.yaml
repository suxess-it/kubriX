apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: backstage-probe
spec:
  interval: 60s
  module: https_2xx_insecure
  prober:
    url: sx-prometheus-blackbox-exporter.prometheus-blackbox-exporter.svc:9115
    path: /probe
  targets:
    staticConfig:
      labels:
        service: backstage
        platform: kubrix
      static:
        - https://{{ .Values.backstage.ingress.host }}/.backstage/health/v1/liveness
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: backstage-kubrix-service-status
  namespace: mimir
  labels:
    platform: kubrix
    service: backstage
spec:
  groups:
  - name: kubrix.service.status
    interval: 30s
    rules:
    - record: kubrix_service_status
      labels:
        platform: kubrix
        service: backstage
      expr: |
        (
          2 * (scalar(min(probe_success{platform="kubrix",service="backstage"})) == bool 0)
        )
        +
        (
          scalar(max(GRAFANA_ALERTS{namespace="backstage"} == bool 1) or vector(0))
          *
          (1 - (scalar(min(probe_success{platform="kubrix",service="backstage"})) == bool 0))
        )
apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    grafana_dashboard: "1"
  annotations:
    argocd.argoproj.io/sync-options: ServerSideApply=true
  name: argocd-dashboards
data:
{{ (.Files.Glob "dashboard-files/*").AsConfig | indent 2 }}

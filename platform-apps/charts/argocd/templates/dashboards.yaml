apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    grafana_dashboard: "1"
  annotations:
    argocd.argoproj.io/sync-options: ServerSideApply=true
    grafana_folder: {{ required "grafanaFolder must be set" .Values.grafanaFolder | quote }}
  name: argocd-dashboards
data:
{{- $files := .Files.Glob "dashboard-files/*" }}
{{- range $path, $_ := $files }}
  {{ base $path }}: |-
{{ tpl ($.Files.Get $path) $ | nindent 4 }}
{{- end }}

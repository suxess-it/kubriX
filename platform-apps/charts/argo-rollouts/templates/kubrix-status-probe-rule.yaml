apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: argo-rollouts-kubrix-service-status
  namespace: mimir
  labels:
    platform: kubrix
    service: argo-rollouts
spec:
  groups:
  - name: kubrix.service.status
    interval: 30s
    rules:
    - record: kubrix_service_status
      labels:
        platform: kubrix
        service: argo-rollouts
      expr: |
        (
          2 * (scalar(min(probe_success{platform="kubrix",service="argo-rollouts"})) == bool 0)
        )
        +
        (
          scalar(max(GRAFANA_ALERTS{namespace="argo-rollouts"} == bool 1) or vector(0))
          *
          (1 - (scalar(min(probe_success{platform="kubrix",service="argo-rollouts"})) == bool 0))
        )
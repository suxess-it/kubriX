{{- if .Values.kubrixAlerts.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    grafana_alert: "1"
  name: kubrix-alerts
data:
{{ (.Files.Glob "alerting-rules/kubrix-alerts.yaml").AsConfig | indent 2 }}
---
{{ end -}}
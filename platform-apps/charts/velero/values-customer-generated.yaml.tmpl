backupStorageLocation:
  config:
    s3Url: https://sx-minio.minio.svc.cluster.local:9000
    bucket: velero
    region: minio
    insecureSkipTLSVerify: true # temp for minio/local deployment
    publicUrl: https://minio.{{ .kubriX.domain }}
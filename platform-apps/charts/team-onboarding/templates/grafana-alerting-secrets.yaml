{{- $kubernetesRbac := .Values.kubernetesRbac | default dict }}
{{- $spoke := $kubernetesRbac.spoke | default dict }}
{{- if and (gt (len (include "team-onboarding.teams" . | fromYamlArray)) 0) (not ($spoke.enabled | default false)) }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: team-onboarding-alerting-secret
  namespace: grafana
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: openbao-backend
    kind: ClusterSecretStore
  target:
    name: team-onboarding-alerting-secret
    deletionPolicy: "Merge"
  dataFrom:
{{- range (include "team-onboarding.teams" . | fromYamlArray) }}
    - extract:
        key: {{ .name }}/observability
      rewrite:
        - regexp:
            source: "^(.*)$"
            target: "{{ .name | upper | replace "-" "_" }}_$1"
{{- end }}
{{- end }}

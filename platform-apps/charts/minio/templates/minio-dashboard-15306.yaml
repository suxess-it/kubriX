apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    grafana_dashboard: "1"
  name: minio-dashboard-15306
  annotations:
    grafana_folder: {{ required "grafanaFolder must be set" .Values.grafanaFolder | quote }}
data:
  minio-dashboard-15306.json: |-
{{ .Files.Get "dashboard-files/minio-dashboard-15306.json" | indent 4 }}
